version: '3.8'

services:
  web:
    build: .
    container_name: hello-flask-web-1
    ports:
      - "5005:5005"
    depends_on:
      - mydb

  mydb:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: my-secret-pw